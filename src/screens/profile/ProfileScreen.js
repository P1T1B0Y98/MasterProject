import React from 'react';
import { InnerContainer, StyledContainer, PageTitle } from '../../components/styles';

export const ProfileScreen = () => {
    return (
        <StyledContainer>
            <InnerContainer>
                <PageTitle>Profile</PageTitle>
            </InnerContainer>
        </StyledContainer>
    )
}